<ul id="comments_list_<%=@post.id%>">
<% if @post.comments.size > 0 %>
  <% @post.comments.each do |comment| %>
    <div class="well">
      <%= render comment %>
    </div>
  <% end %>
<% else %>
    <p>No comments</p>
<% end %>
</ul>
